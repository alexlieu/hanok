/**
 * Layout constants for consistent spacing and sizing across the application
 */

/**
 * Height of the sticky header on the Checkout page
 * Used for scroll-padding-top and scroll-margin calculations
 */
export const STICKY_HEADER_HEIGHT = "4.5rem"; // 72px

/**
 * Height of the sticky header in pixels (for calculations that need numeric values)
 */
export const STICKY_HEADER_HEIGHT_PX = 72;
